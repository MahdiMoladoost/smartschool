# چک‌لیست اصلاحات پروژه مدرسه هوشمند

## انجام‌شده

- [x] رفع خطای Syntax در `public/assets/js/admin.js`.
- [x] یکپارچه‌سازی توابع تاریخ شمسی و تبدیل اعداد فارسی/انگلیسی در پنل مدیر.
- [x] رفع تداخل چند تابع تکراری در `admin.js` تا فایل بدون خطای JavaScript اجرا شود.
- [x] حذف کلیدهای API و رمزهای ثابت از کدهای بک‌اند و فرانت‌اند.
- [x] انتقال فراخوانی‌های AI از مرورگر به API داخلی سرور.
- [x] اضافه کردن `.env.example` تمیز و قابل تنظیم.
- [x] به‌روزرسانی `.gitignore` برای جلوگیری از انتشار `.env`، `node_modules`، لاگ‌ها، آپلودها و فایل‌های موقت.
- [x] حذف fallback ناامن `JWT_SECRET` و جایگزینی با متغیر محیطی.
- [x] حذف رمز پیش‌فرض دیتابیس از فایل‌های پروژه و اتصال همه‌چیز به `.env`.
- [x] ثبت routeهای رأی‌گیری/polls که قبلاً فقط import شده بودند.
- [x] کنترل مسیرهای تکراری Express در زمان ثبت routeها تا آخرین تعریف معتبر فعال شود.
- [x] امکان بالا آمدن سرور در حالت توسعه حتی وقتی MySQL فعال نیست (`REQUIRE_DB=false`).
- [x] بازنویسی `seed-data.js` از SQLite به MySQL.
- [x] هماهنگ‌سازی `init-db.js` با seed جدید MySQL.
- [x] اضافه کردن APIهای سازگاری که فرانت‌اند صدا می‌زد ولی در بک‌اند نبودند:
  - [x] بازنشانی رمز کاربر مدیر
  - [x] ورود موقت مدیر به حساب کاربران
  - [x] تأیید/رد ثبت‌نام‌ها
  - [x] داشبورد دانش‌آموز
  - [x] درخواست‌های مرخصی دانش‌آموز
  - [x] مدیریت سوالات آزمون معلم
  - [x] آپلود آواتار معلم
  - [x] ارتباط معلم با والدین
  - [x] گزارش دانش‌آموز برای معلم
  - [x] دریافت نمرات آزمون معلم
- [x] اصلاح فایل‌ها و لینک‌های استاتیک خراب یا ناسازگار:
  - [x] `load-assets.js`
  - [x] `pages/load-assets.js`
  - [x] `pages/js/core/security.js`
  - [x] `public/assets/css/home page/contact-advanced.css`
  - [x] مسیر تصویر نمونه در صفحه اصلی
  - [x] لینک‌های قدیمی صفحه اصلی، ورود، ثبت‌نام و خدمات
  - [x] routeهای redirect برای مسیرهای قدیمی مثل `/pages/homepage/homepage` و `/frontend/html/login.html`

## تست‌شده

- [x] بررسی Syntax تمام فایل‌های JavaScript/MJS با Node.
- [x] اجرای `npm run panels:validate` و اعتبارسنجی ۵۲ صفحه پنل.
- [x] تست بالا آمدن سرور بدون MySQL در حالت توسعه.
- [x] تست endpoint سلامت سرور: `/api/health`.
- [x] جستجو برای کلیدها و رمزهای لو رفته قبلی در سورس پروژه.
- [x] بررسی مسیرهای استاتیک غیر فونت و رسیدن تعداد missing asset به صفر.

## وابسته به محیط اجرا

- [ ] تست کامل CRUD و APIهای دیتابیسی نیاز به MySQL فعال و `.env` واقعی دارد.
- [ ] تست AI نیاز به مقدار واقعی `AI_API_KEY` روی سرور دارد.
- [ ] تست پیامک نیاز به مقدار واقعی `KAVENEGAR_API_KEY` دارد.

## روش اجرای پیشنهادی

```bash
npm install
cp .env.example .env
# مقدارهای .env را تنظیم کنید
npm run db:seed
npm start
```

برای توسعه بدون دیتابیس، مقدار `REQUIRE_DB=false` بماند. برای محیط واقعی/هاست، `REQUIRE_DB=true` و `NODE_ENV=production` توصیه می‌شود.
