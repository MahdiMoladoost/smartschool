# قرارداد رابط و تعاملات پنل معلم

این سند منبع پذیرش پنل معلم است. همه صفحات از `layout.template.html`، هسته `teacher-portal.js` و design system فایل `teacher-portal.css` استفاده می‌کنند.

## الگوهای مشترک

- هدر: دکمه منو در دسکتاپ سایدبار را فشرده و در موبایل drawer را باز می‌کند؛ بروزرسانی صفحه handler همان صفحه را دوباره اجرا می‌کند؛ اعلان‌ها drawer اعلانات را باز می‌کنند؛ تمام‌صفحه از Fullscreen API استفاده می‌کند.
- سایدبار: لوگو و نام مدرسه از `GET /api/v1/settings` و نام/تصویر معلم از `GET /api/v1/teacher/profile` دریافت می‌شود.
- مودال: `Escape`، کلیک روی پس‌زمینه و دکمه بستن مودال را می‌بندند؛ focus داخل مودال محصور و پس از بستن به دکمه آغازگر برمی‌گردد.
- عملیات مخرب: پیش از حذف یا خروج، مودال تأیید نمایش داده می‌شود.
- فرم‌ها: فیلد الزامی، محدودیت عددی، loading، success، error و retry دارند.
- جدول‌ها: جستجو، empty state، نمایش واکنش‌گرا و در صفحات داده‌محور خروجی CSV دارند.

## داشبورد و کلاس

| صفحه | اقدام کاربر | نتیجه و فیلدها | API |
|---|---|---|---|
| داشبورد | کارت کلاس/دانش‌آموز/تکلیف/نمره | انتقال به صفحه متناظر | `GET /teacher/dashboard`, `/teacher/classes`, `/teacher/assignments`, `/teacher/exams`, `/teacher/schedule`, `/teacher/announcements` |
| داشبورد | ثبت حضور، ثبت نمره، ایجاد تکلیف | انتقال مستقیم به جریان عملیاتی | مسیرهای داخلی پنل |
| برنامه هفتگی | فیلتر روز و کلاس | جدول برنامه بلافاصله فیلتر می‌شود | `GET /teacher/schedule` |
| برنامه هفتگی | چاپ / خروجی | نمای چاپ یا فایل CSV | سمت کاربر |
| کلاس‌های من | «جزئیات» | مودال با KPI، تب دانش‌آموزان و تب دروس | `GET /teacher/classes/:id` |
| کلاس‌های من | «دانش‌آموزان» | انتقال با `class_id` انتخاب‌شده | `GET /teacher/students?class_id=` |
| دانش‌آموزان | «پرونده» | مودال مشخصات، KPI، تب نمرات و تب حضور | `GET /teacher/student-report/:id`, `GET /teacher/parent/:id` |
| دانش‌آموزان | «پیام» | پیام‌رسان با مخاطب از پیش انتخاب‌شده | `GET /teacher/messages/:id`, `POST /teacher/messages` |

## حضور و نمرات

| صفحه | اقدام کاربر | نتیجه و فیلدها | API |
|---|---|---|---|
| ثبت حضور | انتخاب کلاس، تاریخ و زنگ | دانش‌آموزان همان جلسه بارگذاری می‌شوند | `GET /teacher/attendance` |
| ثبت حضور | همه حاضر/غایب/تأخیر | وضعیت تمام ردیف‌ها تغییر می‌کند؛ هنوز ذخیره نمی‌شود | سمت کاربر |
| ثبت حضور | ذخیره | مودال خلاصه تعداد وضعیت‌ها؛ پس از تأیید ثبت نهایی | `POST /teacher/attendance` |
| سوابق حضور | بارگذاری | جدول فقط خواندنی و KPI وضعیت‌ها | `GET /teacher/attendance` |
| حضور و غیاب | خروجی | CSV نام، وضعیت و توضیح | سمت کاربر |
| ثبت/ویرایش نمرات | انتخاب کلاس، درس، نوبت و نوع | دفتر نمره همان ارزیابی بارگذاری می‌شود | `GET /teacher/courses`, `GET /teacher/grades` |
| ثبت/ویرایش نمرات | نمره گروهی | مقدار فقط در خانه‌های خالی اعمال می‌شود | سمت کاربر |
| ثبت/ویرایش نمرات | ذخیره همه | مودال تعداد نمرات؛ سپس ثبت گروهی صفر تا بیست | `POST /teacher/grades/bulk` |
| دفتر نمرات | خروجی | CSV دانش‌آموز، نام کاربری و نمره | سمت کاربر |

## تکالیف و آزمون

| صفحه | اقدام کاربر | مودال و فیلدها | API |
|---|---|---|---|
| مدیریت/ایجاد تکلیف | ایجاد تکلیف | عنوان، کلاس، درس، مهلت، نمره کل، شرح | `POST /teacher/assignments` |
| مدیریت تکلیف | جزئیات | کلاس، درس، مهلت، بارم و شرح | داده جدول |
| مدیریت تکلیف | ویرایش | همان مودال ایجاد با داده فعلی | `PUT /teacher/assignments/:id` |
| مدیریت تکلیف | حذف | مودال تأیید حذف قطعی | `DELETE /teacher/assignments/:id` |
| تصحیح تکالیف | تصحیح/ویرایش ارزیابی | پیش‌نمایش متن/فایل، نمره، بازخورد چندخطی | `GET /teacher/assignments/:id/submissions`, `PUT /teacher/submissions/:id` |
| ایجاد/مدیریت آزمون | ایجاد آزمون | عنوان، کلاس، درس، شروع، مدت، بارم، وضعیت انتشار و شرح | `POST /teacher/exams` |
| مدیریت آزمون | جزئیات/ویرایش/حذف | مودال جزئیات، مودال ویرایش یا تأیید حذف | `PUT/DELETE /teacher/exams/:id` |
| مدیریت آزمون | سؤال‌ها | مودال سؤال با متن، نوع، بارم، گزینه‌ها و پاسخ صحیح | `GET /teacher/exams/:id/questions`, `POST/PUT/DELETE /teacher/exam-questions` |
| نتایج آزمون | انتخاب آزمون | KPI میانگین/کمینه/بیشینه و جدول شرکت‌کنندگان | `GET /teacher/exams/:id/grades` |
| نتایج آزمون | جزئیات | مودال زمان، نمره، درصد و پاسخ‌های ثبت‌شده | داده نتیجه آزمون |

## محتوا و کلاس آنلاین

| صفحه | اقدام کاربر | مودال و فیلدها | API |
|---|---|---|---|
| کلاس آنلاین | ایجاد | عنوان، زمان، وضعیت، پیوند ورود، توضیحات | `POST /teacher/class-events` |
| کلاس مجازی | ایجاد | همان فرم با نوع `virtual_class` | `POST /teacher/class-events` |
| کلاس آنلاین/مجازی | ورود/کپی | بازکردن امن پیوند یا کپی در clipboard | سمت کاربر |
| کلاس آنلاین/مجازی | ویرایش/حذف | مودال ویرایش یا تأیید حذف | `PUT/DELETE /teacher/class-events/:id` |
| بارگذاری محتوا | بارگذاری | عنوان، کلاس، درس، سطح دسترسی، توضیح و فایل تا ۱۰MB | `POST /teacher/library/upload` |
| کتابخانه | مشاهده | بازکردن فایل در تب امن | مسیر فایل ثبت‌شده |
| کتابخانه | ویرایش مشخصات | عنوان، کلاس، درس، دسترسی و توضیح؛ فایل عوض نمی‌شود | `PUT /teacher/library/:id` |
| کتابخانه | حذف | تأیید و حذف رکورد و فایل محلی | `DELETE /teacher/library/:id` |

## ارتباطات، گزارش و پیگیری

| صفحه | اقدام کاربر | نتیجه و فیلدها | API |
|---|---|---|---|
| اطلاعیه کلاس | اطلاعیه جدید | مودال کلاس، اولویت، عنوان و متن با شمارنده | `POST /teacher/announcements` |
| اطلاعیه کلاس | تأیید ارسال | مودال تأیید ارسال برای همه دانش‌آموزان کلاس | `POST /teacher/announcements` |
| پیام دانش‌آموز | انتخاب/جستجوی مخاطب | تاریخچه گفتگو و composer دارای شمارنده | `GET /teacher/contacts`, `GET/POST /teacher/messages` |
| بازخورد دانش‌آموز | ارسال | همان پیام‌رسان با عنوان و متن بازخورد | `GET/POST /teacher/messages` |
| پیام مدیریت | ارسال | مخاطبان مدیر/مدیریت مدرسه | `GET /teacher/contacts?role=admin`, `GET/POST /teacher/messages` |
| پیشنهاد مدرسه | ارسال | پیام با برچسب پیشنهاد مدرسه | `POST /teacher/messages` |
| پیام والدین | کلاس، دانش‌آموز، سرپرست | تاریخچه و ارسال پیام با وابستگی دانش‌آموز | `GET /teacher/parent/:id`, `GET /teacher/parent-messages/:id`, `POST /teacher/send-to-parent` |
| گزارش تدریس | انتخاب کلاس/دانش‌آموز/نوع | KPI و جدول نمرات یا حضور | `GET /teacher/student-report/:id` |
| کارنامه دانش‌آموز | همان جریان گزارش | چاپ و خروجی CSV | `GET /teacher/student-report/:id` |
| تحلیل عملکرد کلاس | همان جریان گزارش | KPI میانگین، بیشترین و نیازمند توجه | `GET /teacher/student-report/:id` |
| گزارش انضباطی | ثبت/ویرایش | مودال کلاس، دانش‌آموز، امتیاز، عنوان و شرح | `POST/PUT /teacher/student-activities` |
| ثبت تشویقی | ثبت/ویرایش | همان مودال با نوع تشویقی | `POST/PUT /teacher/student-activities` |
| گزارش انضباطی/تشویقی | حذف | مودال تأیید | `DELETE /teacher/student-activities/:id` |

## حساب و هوش مصنوعی

| صفحه | اقدام کاربر | نتیجه و فیلدها | API |
|---|---|---|---|
| پروفایل | ذخیره | نام، تلفن، ایمیل و تصویر پروفایل | `PUT /teacher/profile`, `POST /teacher/profile/avatar` |
| تغییر رمز | ثبت | رمز فعلی، رمز جدید و تکرار؛ تطبیق سمت کاربر | `POST /auth/change-password` |
| آزمون‌ساز هوشمند | پردازش | موضوع، مبحث، پایه، تعداد و دشواری | `POST /ai/teacher/quiz-generator` |
| تحلیل دانش‌آموز | پردازش | کلاس و دانش‌آموز | `POST /ai/teacher/performance-summary` |
| سؤال از کتاب/PDF، پاسخنامه، تصحیح خودکار، سؤال تکمیلی، تحلیل سختی | پردازش | موضوع و منبع/دستور؛ خروجی قابل ویرایش، کپی و دریافت | `POST /ai/assist` |

## معیارهای مرجع UX

- Canvas Instructor Guide: حضور، تحلیل، پیام مبتنی بر وضعیت و مشاهده جزئیات دانش‌آموز.
- Google Classroom Teacher Help: ایجاد تکلیف، مشاهده کار دانش‌آموز، نمره، بازخورد و بازگرداندن کار.
- Moodle Docs: جداسازی تنظیمات آزمون از ساخت سؤال و مدیریت سؤال‌ها.

این الگوها صرفاً برای تکمیل تجربه کاربری استفاده شده‌اند؛ مدل داده، نقش‌ها و دسترسی‌ها متعلق به همین پروژه هستند.
